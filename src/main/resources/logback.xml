<configuration>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Hibernate logs -->
    <logger name="org.hibernate.SQL" level="DEBUG" /> <!-- Logs raw SQL -->
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" /> <!-- Logs parameter values -->

</configuration>